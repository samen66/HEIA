# Copy to backend/.env and set your key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Default model (gemini-1.5-flash is deprecated — use 2.5+)
# gemini-2.5-flash has a low free-tier daily cap; flash-latest is a good fallback
GEMINI_MODEL=gemini-flash-latest
